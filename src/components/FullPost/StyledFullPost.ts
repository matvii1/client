import { Grid } from '@mui/material'
import { blueGrey } from '@mui/material/colors'
import { styled } from '@mui/material/styles'

export const FullPostText = styled(Grid)(() => ({
  marginTop: '1.25rem',
}))

export const avatarStyles = {
  fontSize: '0.8rem',
  width: '30px',
  height: '30px',
  backgroundColor: blueGrey[500],
}
